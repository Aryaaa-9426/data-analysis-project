# statical analysis 
## what i learnt today 
1. mean 
-finds avg value of an array 
-functions 
np.mean(array)
2. median 
-finds the middle value after sorting the data 
sortig means the arrays are arranged in aascending order
np.median(array)
3. std deviation 
- np.std(array)
4. maximum value 
return highest value value 
np.max(array)
5. minimum value 
returns lowest value 
np.min(array)
6. argmax 
np.argmax(array)
returns the index of the maximum value 
7. argmin 
returns the index of the minimum value 
np.argmin(array)
axix=0
column-wise operation 
arr=np.array
np.mean(arr)
np.mean(arr , axis=0)
calculate the mean of each column 

axis= 1(row wise)
np.mean(arr , axis =1)
calculates the mean of each row